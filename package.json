{
  "name": "agent-alignment-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-log.mjs",
    "summarize": "node scripts/summarize.mjs",
    "promote-rule": "node scripts/promote-rule.mjs",
    "check": "npm run validate && npm run summarize"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
